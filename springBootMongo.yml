apiVersion: v1.19.15-eks-9c63c4/v1
kind: Service
metadata:
  name: frontend-service-stable
spec:
  type: LoadBalancer
  selector:
    app: myportfolio
    type: frontend
    track: stable
  ports:
  - protocol: TCP
    port: 80
    nodePort: 30008
    targetPort: 4000

---
  
apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-deployment-canary
  labels:
    app: myportfolio
    type: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myportfolio
      type: frontend
      track: stable
  template:
    metadata:
      labels:
        app: myportfolio
        type: frontend
        track: stable
    spec:
      containers:
      - name: portfolio
        image: josh1991/king-httpd:king-httpd
        ports:
        - containerPort: 4000
        livenessProbe:
          httpGet:
            path: /
            port: 4000
          initialDelaySeconds: 15
          timeoutSeconds: 1
          periodSeconds: 10
